@model IEnumerable<LoginTest.Models.Friendship>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FriendshipId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FirstUserId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SecondUserId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FriendshipId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FriendshipId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FriendshipId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
